export default {
  state: {},
  mutations: {
    scanTags (state) {
      // hexo.listPostFiles()
      console.log('scanTags...')
    }
  },
  actions: {
    // syncTags ({commit, state}) {
    //   // let openRequest = window.indexedDB.open('music')
    // }
  }
}
